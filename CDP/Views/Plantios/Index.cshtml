@model IEnumerable<CDP.Models.Plantio>

@{
    ViewData["Title"] = "Plantio";
}

<div class="container-fluid px-4">
    <div class="row border-bottom mt-2 md-4">
        <div class="col-lg-10 col-sm-6">
            <h2 class="" style="font-weight:bolder"><i class="bi bi-people"></i>@ViewData["Title"]</h2>
        </div>
        <div class="col-sm-6 col-md-2">
            <a asp-action="Create" class="btn btn-primary">
                Novo Plantio
            </a>
        </div>
    </div>
    <table class="mt-4 table table-hover">
        <thead class="table-primary">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DataPlantio)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Cultura)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Safra)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DistSementes)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Adubacao)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.DataPlantio)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cultura)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Safra)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DistSementes)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Adubacao)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id"><i class="btn bi bi-pencil-square"></i></a> |
                        <a asp-action="Details" asp-route-id="@item.Id"><i class="btn bi bi-card-text"></i></a> |
                        <a asp-action="Delete" asp-route-id="@item.Id"><i class="btn bi bi-trash3"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}